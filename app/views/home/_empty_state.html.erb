<%# 빈 상태 - 가족에 사진이 없을 때 %>
<%= render Sketch::EmptyStateComponent.new(
  icon: :image,
  title: "아직 사진이 없어요",
  description: "소중한 순간을 가족과 공유해보세요.",
  action_text: "첫 사진 업로드",
  action_href: @family ? new_family_photo_path(@family) : "#"
) %>
