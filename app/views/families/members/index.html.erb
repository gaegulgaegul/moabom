<%# Frame0 Sketch Design System - Family Members %>
<%= render Sketch::ContainerComponent.new(padding: :md, variant: :paper) do %>
  <h1 class="text-xl font-sketch font-bold text-sketch-ink mb-6">가족 구성원</h1>

  <!-- 구성원 목록 -->
  <%= render Sketch::CardComponent.new(variant: :default, padding: :none) do %>
    <div class="divide-y-2 divide-sketch-cream">
      <%= render partial: "families/members/member", collection: @memberships, as: :membership %>
    </div>
  <% end %>
<% end %>
