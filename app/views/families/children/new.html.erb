<%# Frame0 Sketch Design System - Add Child %>
<%= render Sketch::ContainerComponent.new(padding: :lg, variant: :paper) do %>
  <h1 class="text-2xl font-sketch font-bold text-sketch-ink mb-6">아이 추가</h1>

  <%= render "form" %>
<% end %>
