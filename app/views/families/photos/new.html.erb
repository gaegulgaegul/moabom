<div class="container mx-auto px-4 py-8">
  <div class="mb-4">
    <%= link_to "← 타임라인으로", family_photos_path(@family), class: "text-blue-600" %>
  </div>

  <h1 class="text-2xl font-bold mb-6">사진 업로드</h1>

  <%= render partial: "form", locals: { photo: @photo } %>
</div>
