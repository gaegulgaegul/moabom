<div class="container mx-auto px-4 py-8">
  <div class="mb-4">
    <%= link_to "← 사진으로", family_photo_path(@family, @photo), class: "text-blue-600" %>
  </div>

  <h1 class="text-2xl font-bold mb-6">사진 수정</h1>

  <%= render partial: "form", locals: { photo: @photo } %>
</div>
