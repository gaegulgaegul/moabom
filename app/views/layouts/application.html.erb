<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Moabom" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Moabom">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Frame0 Sketch Style Fonts %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Architects+Daughter&family=Caveat:wght@400;500;600;700&family=Comic+Neue:wght@300;400;700&family=Gaegu:wght@300;400;700&family=Gamja+Flower&family=Gloria+Hallelujah&family=Hi+Melody&family=Indie+Flower&family=Kalam:wght@300;400;700&family=Nanum+Pen+Script&family=Patrick+Hand&family=Poor+Story&display=swap" rel="stylesheet">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%# CDN Fallback for Sketch Dependencies %>
    <%# Timeout-based verification: check if rough loaded after importmap processing %>
    <script>
      (function() {
        var FALLBACK_DELAY = 2000;
        var fallbackLoaded = false;

        function loadRoughFallback() {
          if (fallbackLoaded || window.rough) return;
          fallbackLoaded = true;
          console.warn('Rough.js not available, loading CDN fallback...');
          var script = document.createElement('script');
          script.src = 'https://cdn.jsdelivr.net/npm/roughjs@4.6.6/bundled/rough.min.js';
          document.head.appendChild(script);
        }

        setTimeout(function() {
          if (!window.rough) loadRoughFallback();
        }, FALLBACK_DELAY);
      })();
    </script>
  </head>

  <body class="bg-sketch-paper">
    <% if logged_in? %>
      <%= render "shared/header" %>
    <% end %>

    <main class="<%= logged_in? ? (show_bottom_tabbar? ? 'pt-14 pb-20' : 'pt-14') : '' %> min-h-screen">
      <% if flash[:notice] %>
        <div class="mx-4 mt-4">
          <%= render Sketch::AlertComponent.new(variant: :success, dismissible: true) do %>
            <%= flash[:notice] %>
          <% end %>
        </div>
      <% end %>

      <% if flash[:alert] %>
        <div class="mx-4 mt-4">
          <%= render Sketch::AlertComponent.new(variant: :error, dismissible: true) do %>
            <%= flash[:alert] %>
          <% end %>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <%= render "shared/tabbar" if show_bottom_tabbar? %>
  </body>
</html>
