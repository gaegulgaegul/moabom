<div class="invitation-page">
  <h1><%= @invitation.family.name %> 가족에 초대되었습니다</h1>

  <p>
    <strong><%= @invitation.inviter.nickname %></strong>님이
    <%= @invitation.family.name %> 가족에 초대했습니다.
  </p>

  <% if logged_in? %>
    <%= button_to "가족에 참여하기", accept_invitation_path(@invitation.token), method: :post, class: "btn btn-primary" %>
  <% else %>
    <p>가족에 참여하려면 먼저 로그인해주세요.</p>
    <%= button_to "로그인 후 참여하기", accept_invitation_path(@invitation.token), method: :post, class: "btn btn-primary" %>
  <% end %>
</div>
